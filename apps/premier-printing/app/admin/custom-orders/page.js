import { CustomOrdersList } from "@pythias/backend";

export const metadata = { title: "Custom Orders — Premier Printing" };

export default function CustomOrdersPage() {
    return <CustomOrdersList />;
}
